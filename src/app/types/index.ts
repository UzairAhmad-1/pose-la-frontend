export interface EmotionalSpace {
  id: string;
  title: string;
  description: string;
  ctaText: string;
}

export interface ComplementaryModule {
  id: string;
  title: string;
  description: string;
}
